// Intentionally empty — the bottom-bar work was consolidated into
// components/app/mobile-nav.tsx. This stub re-exports the canonical
// component so any straggler import keeps working.

export { MobileBottomNav as StandaloneTabBar } from "./mobile-nav";
