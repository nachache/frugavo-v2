// Deprecated — superseded by the static waving-hand glyph rendered
// server-side inside HomeHeroBand. Kept as an empty module to avoid
// breaking any external imports during the migration window.

export {};
